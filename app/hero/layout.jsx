import { PageWrapper } from "@/components/page-wrapper";

export default function DashboardLayout({ children }) {
  return (
    
    <section className="">
      
      {children}
      
    </section>
  );
}
